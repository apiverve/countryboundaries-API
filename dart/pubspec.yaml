name: apiverve_countryboundaries
description: Country Boundaries is a simple tool for getting the shape of a specific country's border. It returns the shape of the specified country's border.
version: 1.1.14
homepage: https://apiverve.com/marketplace/countryboundaries?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/countryboundaries-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
